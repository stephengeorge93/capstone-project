SELECT
    PROPERTY_ID,
    PROPERTY_TYPE,
    BEDROOMS,
    BATHROOMS,
    SQUARE_FOOTAGE,
    LOT_SIZE,
    YEAR_BUILT,
    FLOOR_COUNT,
    GARAGE,
    GARAGE_SPACES,
    GARAGE_TYPE,
    HEATING_TYPE,
    COOLING_TYPE,
    ROOF_TYPE,
    UNIT_COUNT,
    ROOM_COUNT,
    ARCHITECTURE_TYPE
FROM {{ ref('stg_property_data') }}
